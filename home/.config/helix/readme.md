# Helix Editor usage notes

## Language servers

## js, jsx, ts, tsx files

These require `typescript-language-server` and `typescript` to be installed globally on the system.

My preferred way is:

```bash
volta install typescript-language-server typescript
```
